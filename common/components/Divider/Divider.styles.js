import {ScaledSheet} from 'react-native-size-matters';
import {colors} from '../../colors';

export const dividerStyles = ScaledSheet.create({
  divider: {
    height: 1,
    backgroundColor: colors.WHITE_LIGHT,
  },
});
